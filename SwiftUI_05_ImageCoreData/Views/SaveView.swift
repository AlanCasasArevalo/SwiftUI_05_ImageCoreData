

import SwiftUI

struct SaveView: View {
    var body: some View {
        Text("Hello, SaveView")
    }
}

struct SaveView_Previews: PreviewProvider {
    static var previews: some View {
        SaveView()
    }
}
